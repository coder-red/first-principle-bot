<!-- One concern per PR. A refactor bundled with a fix is hard to review and
     harder to revert. -->

**What this changes**

**Why**

**How it was tested**

<!-- Name the model if you tested against one — deck behaviour is
     model-specific. -->

- [ ] `python -m pytest tests/ -q`
- [ ] `python tests/browser/run_all.py` (if anything the reader touches changed)
- [ ] `CHANGELOG.md` updated under Unreleased (if a user would notice)

**Anything a reviewer should look at twice**

<!-- Especially if this touches fpb/deck.py, fpb/limits.py or fpb/auth.py, or
     assumes more than one process. -->
